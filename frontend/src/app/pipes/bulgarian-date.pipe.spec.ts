import { BulgarianDatePipe } from './bulgarian-date.pipe';

describe('BulgarianDatePipe', () => {
  it('create an instance', () => {
    const pipe = new BulgarianDatePipe();
    expect(pipe).toBeTruthy();
  });
});
